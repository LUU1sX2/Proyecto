<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<StackPane xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="tu.paquete.ControladorTriangulo">
    <VBox alignment="CENTER" spacing="10">

        <!-- Fila 1 -->
        <HBox alignment="CENTER" prefWidth="158.0" spacing="10">
            <Button fx:id="ficha1" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
        </HBox>

        <!-- Fila 2 -->
        <HBox alignment="CENTER" spacing="10">
            <Button fx:id="ficha2" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha3" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
        </HBox>

        <!-- Fila 3 -->
        <HBox alignment="CENTER" spacing="10">
            <Button fx:id="ficha4" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha5" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha6" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
        </HBox>

        <!-- Fila 4 -->
        <HBox alignment="CENTER" spacing="10">
            <Button fx:id="ficha7" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha8" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha9" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha10" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
        </HBox>

        <!-- Fila 5 -->
        <HBox alignment="CENTER" spacing="10">
            <Button fx:id="ficha11" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha12" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha13" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha14" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
            <Button fx:id="ficha15" prefHeight="50" prefWidth="50" text="Ficha" onAction="#moverFicha"/>
        </HBox>

        <!-- Botones de control -->
        <HBox alignment="CENTER" spacing="10" style="-fx-padding: 20 0 0 0;">
            <Button text="Salir al Menú" onAction="#salirAlMenu"/>
            <Button text="Salir del Juego" onAction="#salirDelJuego"/>
            <Button text="Undo" onAction="#deshacer"/>
            <Button text="Reiniciar" onAction="#reiniciar"/>
        </HBox>

    </VBox>
</StackPane>
